const CheckoutContainer = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="">
      <div className="max-w-[1500px] px-5 mx-auto py-10">{children}</div>
    </div>
  );
};

export default CheckoutContainer;
